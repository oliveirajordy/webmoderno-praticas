const INITIAL_STATE = {
    name: 'Usuario Mock',
    email: 'mock@coder.com'
}

export const userReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state
    }
}