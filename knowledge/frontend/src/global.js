export const baseApiUrl = 'http://localhost:3001'
export const userKey = '__knowledge_user'

export const mediaQuery = {
    xs: window.outerWidth <= 576,
    sm: window.outerWidth > 576 && window.outerWidth <= 768,
    md: window.outerWidth > 768 && window.outerWidth <= 960,
    lg: window.outerWidth > 960 && window.outerWidth <= 1140,
    xl: window.outerWidth > 1140
}